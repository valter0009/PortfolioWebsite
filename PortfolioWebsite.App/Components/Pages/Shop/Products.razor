@inherits ProductsBase


@if (Products == null)
{
	<DisplaySpinner/>
}
else
{
	

	var productCount = Products.Count();

	if (productCount > 0)
	{		
			@foreach(var prodGroup in GetFroupedProductsByCategory()){
				<h4>@GetCategoryName(prodGroup)</h4>

			<div class="row mt-3">
				<DisplayProducts Products="@prodGroup.Take(4)"></DisplayProducts>
			</div>
			<hr class="mb-3"/>
			}
			

		
	}
}
